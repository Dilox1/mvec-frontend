import {Link} from 'react-router-dom';import Storefront from '../components/Storefront';import {vendors} from '../data';import Icon from '../components/Icon';import Pagination from '../components/Pagination';import {useState} from 'react';
export default function Vendors(){const [page,setPage]=useState(1);return <Storefront><main className="catalog-page"><div className="catalog-head"><div><span className="eyebrow">MARKETPLACE STORES</span><h1>Trusted vendors</h1><p>Discover local stores and shop directly from verified sellers.</p></div></div><div className="vendor-grid vendor-page-grid">{vendors.slice((page-1)*6,page*6).map(v=><Link to={`/vendors/${v.id}`} className="vendor-card big" key={v.id}><div className="vendor-avatar">{v.name[0]}</div><div><b>{v.name}</b><small>{v.category}</small><span>★ {v.rating} · {v.products} products</span><small>Verified seller · Rwanda</small></div><Icon name="arrow"/></Link>)}</div><Pagination page={page} setPage={setPage} total={vendors.length} perPage={6}/></main></Storefront>}
