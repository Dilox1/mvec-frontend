import {Link} from 'react-router-dom';import Storefront from '../components/Storefront';import {useMarketplace} from '../context/MarketplaceContext';
const money=n=>new Intl.NumberFormat('en-RW').format(n)+' RWF';
export default function Wishlist(){const {wishlist,removeFromCart,addToCart,toggleWishlist}=useMarketplace();return <Storefront><main className="account-page"><div className="page-title"><span className="eyebrow">SAVED ITEMS</span><h1>Wishlist</h1><p>Save products and get notified when prices drop.</p></div><div className="wishlist-grid">{wishlist.map(p=><div className="wish-card" key={p.id}><Link to={`/product/${p.id}`}><img src={p.image} alt=""/></Link><div><small>{p.vendor}</small><Link to={`/product/${p.id}`}><h3>{p.name}</h3></Link><b>{money(p.price)}</b><span className="price-drop">↓ Price drop available</span><div className="wish-actions"><button className="gradient-btn" onClick={()=>{addToCart(p,1);toggleWishlist(p)}}>Move to cart</button><button className="icon-btn" onClick={()=>toggleWishlist(p)}>Remove</button></div></div></div>)}</div>{!wishlist.length&&<div className="empty-state"><h2>Your wishlist is empty</h2><p>Add products by clicking the heart icon.</p><Link className="gradient-btn" to="/shop">Discover products</Link></div>}</main></Storefront>}
